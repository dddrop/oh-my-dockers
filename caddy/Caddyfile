# Caddy configuration for oh-my-dockers
# Project-specific configurations are imported from /etc/caddy/projects/*.caddy
{
	# Global options
	admin 0.0.0.0:2019

	# Disable automatic HTTPS redirects for local development
	auto_https disable_redirects

	# Use local certificates
	local_certs
}

# Import project-specific configurations
import /etc/caddy/projects/*.caddy
